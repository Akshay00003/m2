# Read a string from the user
input_string = input("Enter a string: ")

# Create a list with each character as a separate element
character_list = list(input_string)

# Display the list
print("List of characters:", character_list)
